import { COLOR } from './color';
import { SIZE } from './size';
import { SPACE } from './space';

/**
 * 各共通テーマの定義
 */
export const THEME = {
  color: COLOR,
  size: SIZE,
  space: SPACE,
} as const;
